## Test

```catala
declaration scope Foo:
  output bar content integer

scope Foo:
  definition bar equals 0
```

```catala-test-inline
$ catala Lcalc -s Foo
let scope Foo (Foo_in: Foo_in): Foo =
  let set bar : integer = try 0 with EmptyError -> raise NoValueProvided in
  return { "bar" = bar}_Foo
```
