```catala
declaration scope A:
  output w content integer

scope A:
  definition w equals
    let a equals 1 + 2 in
    #[error.message = "Check that a is negative"]
    assertion a < 0 in
    let b equals a + 3 in
    assertion b = -6 in
    #[error.message = "Check that a is more than -6"]
    assertion not (b > -6) in
    #[error.message = "Attribute not allowed here"]
    b
```

```catala-test-cli
$ catala test-scope A
┌─[WARNING]─
│
│  Attribute #[error.message] is not allowed in this context.
│  It must be put before an assertion or impossible.
│
├─➤ tests/assertions/bad/inline.catala_en:14.5-14.52:
│    │
│ 14 │     #[error.message = "Attribute not allowed here"]
│    │     ‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾
└─
┌─[ERROR]─ 1/3 ─
│
│  Assertion failed: Check that a is negative.
│  The condition resolved to: 3 < 0
│
├─➤ tests/assertions/bad/inline.catala_en:9.5-9.20:
│   │
│ 9 │     assertion a < 0 in
│   │     ‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾
└─
┌─[ERROR]─ 2/3 ─
│
│  Assertion failed.
│  The condition resolved to: 6 = -6
│
├─➤ tests/assertions/bad/inline.catala_en:11.5-11.21:
│    │
│ 11 │     assertion b = -6 in
│    │     ‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾
└─
┌─[ERROR]─ 3/3 ─
│
│  Assertion failed: Check that a is more than -6.
│  The condition resolved to: ~ (6 > -6)
│
├─➤ tests/assertions/bad/inline.catala_en:13.5-13.27:
│    │
│ 13 │     assertion not (b > -6) in
│    │     ‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾‾
└─
#return code 123#
```
