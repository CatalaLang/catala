## Article

```catala
declaration scope A:
  input b content boolean
  input x content integer
  output o content integer

scope A:
  definition o under condition b consequence equals x
```

```catala-test-inline
$ catala DCalc -s A
let scope A (A_in: A_in {b_in: bool; x_in: integer}): A {o: integer} =
  let get b : bool = A_in.b_in in
  let get x : integer = A_in.x_in in
  let set o : integer = error_empty ⟨b ⊢ x⟩ in
  return { A o = o; }
```

```catala-test-inline
$ catala Concolic -s A
[RESULT] Constraints: b_in,
[RESULT] Computation successful! Results:
[RESULT] o = 42
```

