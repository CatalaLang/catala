## Article

```catala
declaration scope A:
  input x content money
  output o condition

scope A:
  rule o under condition x = (money of -1.001)
  consequence fulfilled
```

Here `money of -1.001` is rounded to `-1.01` by Z3 but to `-1.00` by OCaml, so
the condition is not negated properly.

```catala-test-inline
$ catala DCalc -s A
let scope A (A_in: A_in {x_in: money}): A {o: bool} =
  let get x : money = A_in.x_in in
  let set o : bool =
    error_empty
      ⟨ ⟨ ⟨x = to_mon -1.001 ⊢ ⟨true⟩⟩ | false ⊢ ∅ ⟩
      | true ⊢ ⟨ ⟨true ⊢ ⟨false⟩⟩ | false ⊢ ∅ ⟩ ⟩
  in
  return { A o = o; }
```

```catala-test-inline
$ catala Concolic -s A
[RESULT] 
[RESULT] Evaluating with inputs:
[RESULT] . x_in = ¤42.00
[RESULT] Output of scope after evaluation:
[RESULT] . o = false
[RESULT] 
[RESULT] Evaluating with inputs:
[RESULT] . x_in = ¤-1.01
[RESULT] Output of scope after evaluation:
[RESULT] . o = false
[ERROR]
Unexpected error: Failure("[compare_paths] the negated condition lead to the same path")
#return code 125#
```

